# Markdown 表格生成器开发计划

> **功能定位**: 可视化表格编辑器，支持拖拽排序，一键生成 Markdown 表格语法并插入编辑器  
> **集成位置**: 主编辑器工具栏（`components/markdown-editor-client.tsx`）  
> **优先级**: P1（核心功能）/ P2（增强功能）

---

## 一、产品定位

### 核心价值主张

用户痛点：
- ❌ 手动编写 Markdown 表格语法繁琐，容易出错
- ❌ 对齐方式（`:--`, `:--:`, `--:`）难以记忆
- ❌ 调整表格结构（增删行列、调整顺序）需要重写整个表格

解决方案：
- ✅ **可视化编辑** — 所见即所得的表格编辑器
- ✅ **拖拽排序** — 支持拖拽调整行/列顺序（DnD）
- ✅ **实时预览** — 编辑时实时预览 Markdown 表格效果
- ✅ **一键插入** — 生成标准 Markdown 表格语法并插入光标位置

### 用户流程

```
点击工具栏"插入表格"按钮
    ↓
打开表格生成器 Dialog
    ↓
设置初始行列数（默认 3x3）
    ↓
编辑表格内容（支持拖拽调整顺序）
    ↓
设置对齐方式（左/中/右）
    ↓
实时预览 Markdown 表格
    ↓
点击"插入" → 生成 Markdown 语法 → 插入编辑器光标位置
```

---

## 二、技术架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│              Markdown Editor (markdown-editor-client.tsx)        │
├─────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  工具栏: [插入表格] 按钮                                      │  │
│  └──────────────────────┬────────────────────────────────────┘  │
│                         │                                        │
│                         ▼                                        │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │         Table Generator Dialog (Radix Dialog)            │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │  Table Editor (支持 DnD 拖拽)                        │  │  │
│  │  │  ┌──────────┐  ┌──────────┐  ┌──────────┐         │  │  │
│  │  │  │ 单元格   │  │ 单元格   │  │ 单元格   │         │  │  │
│  │  │  │ (可拖拽) │  │ (可拖拽) │  │ (可拖拽) │         │  │  │
│  │  │  └──────────┘  └──────────┘  └──────────┘         │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │  Table Preview (实时预览 Markdown 表格)              │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │  操作栏: [添加行] [添加列] [对齐方式] [插入] [取消]  │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                        │
│                         ▼                                        │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Markdown Table Generator (lib/table-generator.ts)       │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐   │  │
│  │  │ 数据转换     │  │ 对齐处理     │  │ 语法生成        │   │  │
│  │  │ (markdown-  │  │ (align:     │  │ (markdown-table│   │  │
│  │  │  table)     │  │  l/c/r)     │  │ 库)            │   │  │
│  │  └─────────────┘  └─────────────┘  └─────────────────┘   │  │
│  └───────────────────────────────────────────────────────────┘  │
│                         │                                        │
│                         ▼                                        │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  EditorActionsProvider.insertAtCursor()                   │  │
│  │  → 插入生成的 Markdown 表格语法到 CodeMirror 编辑器        │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 模块职责

| 模块 | 职责 | 复用情况 |
|------|------|----------|
| `TableGeneratorDialog` | 表格生成器主弹窗 | 🆕 新建，使用 Radix Dialog |
| `TableEditor` | 表格编辑器（支持 DnD） | 🆕 新建，使用拖拽库 |
| `TableCellInput` | 单元格输入组件 | 🆕 新建 |
| `TablePreview` | Markdown 表格预览 | ✅ 复用 `MarkdownPreview` |
| `TableGenerator` | Markdown 表格语法生成 | 🆕 新建，使用 `markdown-table` 库 |
| `EditorActionsProvider` | 插入内容到编辑器 | ✅ 已存在 |

### 2.3 文件结构

```
components/
├── markdown-table-generator.tsx    # 🆕 表格生成器主组件（Dialog）
├── table-editor.tsx                 # 🆕 表格编辑器（支持 DnD）
├── table-cell-input.tsx              # 🆕 单元格输入组件
└── table-preview.tsx                 # 🆕 表格预览组件（可选，或复用 MarkdownPreview）

lib/
└── table-generator.ts                # 🆕 Markdown 表格生成工具

hooks/
└── use-table-generator.ts            # 🆕 表格生成器 Hook（状态管理）

messages/
└── *.json                            # 新增表格生成器相关翻译
```

---

## 三、技术选型

### 3.1 拖拽库选型（DnD）

**推荐方案：`@dnd-kit/core` + `@dnd-kit/sortable`**

理由：
- ✅ **现代化** — 专为 React 设计，API 简洁
- ✅ **性能优秀** — 使用虚拟化，支持大量元素
- ✅ **无障碍支持** — 内置键盘导航和屏幕阅读器支持
- ✅ **TypeScript 支持** — 完整的类型定义
- ✅ **轻量级** — 相比 `react-beautiful-dnd` 更轻量
- ✅ **维护活跃** — 社区活跃，持续更新

**备选方案：`react-beautiful-dnd`**
- ⚠️ 已停止维护（2023 年）
- ⚠️ 不支持 React 18+ 严格模式
- ❌ 不推荐使用

**安装命令：**
```bash
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

### 3.2 Markdown 表格生成库

**使用：`markdown-table`**

项目已包含该依赖（在 `pnpm-lock.yaml` 中可见），无需额外安装。

**API 示例：**
```typescript
import markdownTable from 'markdown-table'

const table = markdownTable([
  ['Header 1', 'Header 2', 'Header 3'],
  ['Cell 1', 'Cell 2', 'Cell 3'],
  ['Cell 4', 'Cell 5', 'Cell 6']
], {
  align: ['l', 'c', 'r']  // 左对齐、居中、右对齐
})
```

### 3.3 UI 组件库

**使用现有 Radix UI 组件：**
- `@radix-ui/react-dialog` — Dialog 弹窗（已安装）
- `@radix-ui/react-select` — 对齐方式选择器（已安装）
- `@radix-ui/react-button` — 按钮（通过 `components/ui/button.tsx`）

---

## 四、核心功能设计

### 4.1 表格数据结构

```typescript
// lib/table-generator.ts

export interface TableCell {
  id: string           // 唯一标识（用于 DnD）
  content: string      // 单元格内容
}

export interface TableRow {
  id: string           // 唯一标识（用于 DnD）
  cells: TableCell[]   // 该行的所有单元格
}

export interface TableData {
  rows: TableRow[]     // 所有行
  columnCount: number   // 列数（用于验证一致性）
  alignments: ('l' | 'c' | 'r')[]  // 每列的对齐方式
  hasHeader: boolean   // 是否有表头（第一行作为表头）
}
```

### 4.2 拖拽功能设计

#### 4.2.1 行拖拽（Row Drag & Drop）

**功能：** 支持拖拽表格行调整顺序

**实现：**
- 使用 `@dnd-kit/sortable` 的 `useSortable` Hook
- 每行作为可拖拽项（`SortableContext`）
- 拖拽手柄：行号或行左侧图标

**交互：**
```
[≡] Header 1 | Header 2 | Header 3
[≡] Cell 1   | Cell 2   | Cell 3
[≡] Cell 4   | Cell 5   | Cell 6
```

#### 4.2.2 列拖拽（Column Drag & Drop）

**功能：** 支持拖拽表格列调整顺序

**实现：**
- 表头行作为拖拽手柄
- 拖拽时整列（包括表头）一起移动
- 使用 `@dnd-kit/sortable` 的水平排序

**交互：**
```
[≡ Header 1] [≡ Header 2] [≡ Header 3]
   Cell 1       Cell 2       Cell 3
   Cell 4       Cell 5       Cell 6
```

#### 4.2.3 拖拽限制

- ✅ 表头行（如果启用）不可拖拽到数据行位置
- ✅ 拖拽时显示视觉反馈（高亮、阴影）
- ✅ 拖拽完成后自动保存新顺序

### 4.3 Markdown 表格生成逻辑

```typescript
// lib/table-generator.ts

export function generateMarkdownTable(
  data: TableData
): string {
  // 1. 构建表格数据数组
  const tableData: string[][] = data.rows.map(row => 
    row.cells.map(cell => cell.content)
  )

  // 2. 处理对齐方式
  const alignments = data.alignments.map(align => {
    switch (align) {
      case 'l': return 'left'
      case 'c': return 'center'
      case 'r': return 'right'
      default: return 'left'
    }
  })

  // 3. 生成 Markdown 表格
  return markdownTable(tableData, {
    align: alignments
  })
}
```

### 4.4 对齐方式处理

Markdown 表格对齐语法：
- **左对齐**：`:--` 或 `:---`
- **居中**：`:--:` 或 `:---:`
- **右对齐**：`--:` 或 `---:`

`markdown-table` 库会自动处理对齐语法，我们只需传入 `align` 数组。

---

## 五、P1 任务清单

### 🎯 P1 目标

> **一个"易用、直观、支持拖拽"的 Markdown 表格生成器**

P1 成功标准：**用户能在 30 秒内创建并插入一个 3x3 的表格。**

---

### 📋 P1 任务列表

#### Sprint 1: 核心功能（预估 2 天）

| # | 任务 | 优先级 | 预估 | 依赖 |
|---|------|--------|------|------|
| 1.1 | 安装拖拽库 `@dnd-kit/core` + `@dnd-kit/sortable` | P0 | 0.5h | - |
| 1.2 | 创建 `lib/table-generator.ts` 表格生成工具 | P0 | 1h | - |
| 1.3 | 实现 `generateMarkdownTable()` 函数 | P0 | 1h | 1.2 |
| 1.4 | 创建 `components/table-cell-input.tsx` 单元格组件 | P0 | 1h | - |
| 1.5 | 创建 `components/table-editor.tsx` 基础表格编辑器 | P0 | 2h | 1.4 |
| 1.6 | 实现表格 CRUD（增删行列） | P0 | 2h | 1.5 |
| 1.7 | 实现对齐方式选择器 | P0 | 1h | 1.5 |
| 1.8 | 创建 `components/markdown-table-generator.tsx` Dialog | P0 | 2h | 1.5, 1.7 |

#### Sprint 2: 拖拽功能（预估 1.5 天）

| # | 任务 | 优先级 | 预估 | 依赖 |
|---|------|--------|------|------|
| 2.1 | 集成 `@dnd-kit` 到 `table-editor.tsx` | P0 | 2h | Sprint 1 |
| 2.2 | 实现行拖拽排序功能 | P0 | 2h | 2.1 |
| 2.3 | 实现列拖拽排序功能 | P0 | 2h | 2.1 |
| 2.4 | 添加拖拽视觉反馈（高亮、阴影） | P0 | 1h | 2.2, 2.3 |
| 2.5 | 处理拖拽边界情况（表头保护等） | P0 | 1h | 2.2, 2.3 |

#### Sprint 3: 集成与优化（预估 1 天）

| # | 任务 | 优先级 | 预估 | 依赖 |
|---|------|--------|------|------|
| 3.1 | 在 `markdown-editor-client.tsx` 工具栏添加"插入表格"按钮 | P0 | 1h | Sprint 1, 2 |
| 3.2 | 实现插入功能（调用 `EditorActionsProvider.insertAtCursor`） | P0 | 1h | 3.1 |
| 3.3 | 添加实时预览功能（可选，复用 `MarkdownPreview`） | P1 | 2h | 3.1 |
| 3.4 | 响应式布局优化（移动端适配） | P1 | 1h | 3.1 |
| 3.5 | 添加键盘快捷键支持（Tab 切换单元格，Enter 换行等） | P1 | 1h | 1.5 |

#### Sprint 4: i18n + 收尾（预估 0.5 天）

| # | 任务 | 优先级 | 预估 | 依赖 |
|---|------|--------|------|------|
| 4.1 | 添加 5 语言翻译（en/zh/ja/es/fr） | P0 | 1h | - |
| 4.2 | 添加无障碍支持（ARIA 标签、键盘导航） | P1 | 1h | Sprint 2 |
| 4.3 | 错误处理和边界情况测试 | P0 | 1h | Sprint 1-3 |
| 4.4 | 性能优化（大量单元格时的渲染优化） | P1 | 1h | Sprint 2 |

---

### 📊 P1 里程碑

| 里程碑 | 完成标准 | 预估完成 |
|--------|----------|----------|
| M1: 核心功能 | 表格编辑器 + CRUD + 生成 Markdown | Day 2 |
| M2: 拖拽功能 | 行/列拖拽排序完全可用 | Day 3.5 |
| M3: 集成完成 | 工具栏按钮 + 插入功能 + i18n | Day 4.5 |

---

## 六、P2 增强功能（可选）

### 6.1 数据导入

- **CSV 导入**：支持从 CSV 文件导入表格数据
- **Excel 导入**：支持从 Excel 文件导入（需要 `xlsx` 库）
- **粘贴识别**：从剪贴板粘贴表格数据自动识别

### 6.2 表格模板

- **预设模板**：常用表格样式（如：任务列表、价格表、对比表）
- **模板库**：用户可保存自定义模板

### 6.3 高级编辑

- **合并单元格**：支持跨行/跨列合并（需要扩展 Markdown 语法或使用 HTML）
- **格式化**：单元格内支持粗体、斜体等 Markdown 语法
- **公式计算**：简单的表格计算功能（如：求和、平均值）

### 6.4 导出功能

- **导出为 CSV**：将表格数据导出为 CSV 文件
- **导出为 HTML**：生成 HTML 表格代码

---

## 七、核心接口设计

### 7.1 Table Generator Hook

```typescript
// hooks/use-table-generator.ts

export function useTableGenerator(initialRows = 3, initialCols = 3) {
  const [tableData, setTableData] = useState<TableData>({
    rows: generateInitialRows(initialRows, initialCols),
    columnCount: initialCols,
    alignments: Array(initialCols).fill('l'),
    hasHeader: true
  })

  const addRow = useCallback(() => {
    // 添加新行
  }, [])

  const addColumn = useCallback(() => {
    // 添加新列
  }, [])

  const deleteRow = useCallback((rowId: string) => {
    // 删除行
  }, [])

  const deleteColumn = useCallback((colIndex: number) => {
    // 删除列
  }, [])

  const updateCell = useCallback((rowId: string, cellId: string, content: string) => {
    // 更新单元格内容
  }, [])

  const setAlignment = useCallback((colIndex: number, align: 'l' | 'c' | 'r') => {
    // 设置列对齐方式
  }, [])

  const generateMarkdown = useCallback(() => {
    return generateMarkdownTable(tableData)
  }, [tableData])

  return {
    tableData,
    addRow,
    addColumn,
    deleteRow,
    deleteColumn,
    updateCell,
    setAlignment,
    generateMarkdown
  }
}
```

### 7.2 Table Editor 组件接口

```typescript
// components/table-editor.tsx

interface TableEditorProps {
  data: TableData
  onDataChange: (data: TableData) => void
  onCellChange: (rowId: string, cellId: string, content: string) => void
  onRowReorder: (oldIndex: number, newIndex: number) => void
  onColumnReorder: (oldIndex: number, newIndex: number) => void
  onAddRow: () => void
  onAddColumn: () => void
  onDeleteRow: (rowId: string) => void
  onDeleteColumn: (colIndex: number) => void
  onAlignmentChange: (colIndex: number, align: 'l' | 'c' | 'r') => void
}
```

### 7.3 Table Generator Dialog 接口

```typescript
// components/markdown-table-generator.tsx

interface TableGeneratorDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  onInsert: (markdown: string) => void
  initialRows?: number
  initialCols?: number
}
```

---

## 八、技术实现细节

### 8.1 拖拽实现示例（行拖拽）

```typescript
// components/table-editor.tsx

import { DndContext, closestCenter } from '@dnd-kit/core'
import { SortableContext, verticalListSortingStrategy, useSortable } from '@dnd-kit/sortable'
import { CSS } from '@dnd-kit/utilities'

function SortableRow({ row, ...props }) {
  const {
    attributes,
    listeners,
    setNodeRef,
    transform,
    transition,
    isDragging
  } = useSortable({ id: row.id })

  const style = {
    transform: CSS.Transform.toString(transform),
    transition,
    opacity: isDragging ? 0.5 : 1
  }

  return (
    <tr ref={setNodeRef} style={style} {...attributes}>
      <td>
        <button {...listeners}>≡</button>
      </td>
      {/* 其他单元格 */}
    </tr>
  )
}

function TableEditor({ data, onRowReorder }) {
  const handleDragEnd = (event) => {
    const { active, over } = event
    if (active.id !== over.id) {
      const oldIndex = data.rows.findIndex(r => r.id === active.id)
      const newIndex = data.rows.findIndex(r => r.id === over.id)
      onRowReorder(oldIndex, newIndex)
    }
  }

  return (
    <DndContext collisionDetection={closestCenter} onDragEnd={handleDragEnd}>
      <table>
        <SortableContext items={data.rows.map(r => r.id)} strategy={verticalListSortingStrategy}>
          <tbody>
            {data.rows.map(row => (
              <SortableRow key={row.id} row={row} />
            ))}
          </tbody>
        </SortableContext>
      </table>
    </DndContext>
  )
}
```

### 8.2 列拖拽实现

列拖拽需要特殊处理，因为需要同时移动整列的所有单元格：

```typescript
// 列拖拽需要将表格转置处理，或使用自定义拖拽逻辑
// 方案1：转置数据，使用行拖拽逻辑
// 方案2：自定义拖拽处理，手动交换列数据
```

### 8.3 性能优化

- **虚拟化**：如果表格很大（>50 行），考虑使用虚拟滚动
- **防抖**：单元格输入使用防抖，避免频繁更新状态
- **Memo 优化**：使用 `React.memo` 优化单元格组件渲染

---

## 九、测试计划

### 9.1 功能测试

- ✅ 创建表格（默认 3x3）
- ✅ 添加/删除行
- ✅ 添加/删除列
- ✅ 编辑单元格内容
- ✅ 设置对齐方式
- ✅ 行拖拽排序
- ✅ 列拖拽排序
- ✅ 生成 Markdown 语法
- ✅ 插入到编辑器

### 9.2 边界情况测试

- ✅ 空单元格处理
- ✅ 单行/单列表格
- ✅ 大量数据（100+ 行）性能
- ✅ 特殊字符转义（`|`, `\n` 等）
- ✅ 表头行拖拽限制
- ✅ 移动端拖拽体验

### 9.3 无障碍测试

- ✅ 键盘导航（Tab, Enter, Arrow keys）
- ✅ 屏幕阅读器支持
- ✅ 拖拽操作的键盘替代方案

---

## 十、依赖清单

### 10.1 新增依赖

```json
{
  "dependencies": {
    "@dnd-kit/core": "^6.0.0",
    "@dnd-kit/sortable": "^8.0.0",
    "@dnd-kit/utilities": "^3.2.0"
  }
}
```

### 10.2 现有依赖（已安装）

- ✅ `markdown-table` — Markdown 表格生成（已在依赖中）
- ✅ `@radix-ui/react-dialog` — Dialog 组件
- ✅ `@radix-ui/react-select` — 选择器组件
- ✅ `lucide-react` — 图标库

---

## 十一、国际化（i18n）

### 11.1 需要添加的翻译键

```json
{
  "table_generator_title": "插入表格",
  "table_generator_dialog_title": "表格生成器",
  "table_generator_add_row": "添加行",
  "table_generator_add_column": "添加列",
  "table_generator_delete_row": "删除行",
  "table_generator_delete_column": "删除列",
  "table_generator_alignment": "对齐方式",
  "table_generator_align_left": "左对齐",
  "table_generator_align_center": "居中",
  "table_generator_align_right": "右对齐",
  "table_generator_has_header": "包含表头",
  "table_generator_preview": "预览",
  "table_generator_insert": "插入",
  "table_generator_cancel": "取消",
  "table_generator_drag_to_reorder": "拖拽调整顺序"
}
```

---

## 十二、开发注意事项

### 12.1 代码规范

- 遵循项目现有的 TypeScript + React 19 规范
- 使用 2 空格缩进
- 组件文件使用 `kebab-case.tsx` 命名
- 使用 `camelCase` 命名变量和函数

### 12.2 样式规范

- 使用 Tailwind CSS v4
- 复用现有的 UI 组件（`components/ui/`）
- 保持与项目整体设计风格一致

### 12.3 性能考虑

- 大表格（>50 行）考虑虚拟化
- 拖拽操作使用防抖/节流
- 避免不必要的重渲染（使用 `useMemo`, `useCallback`）

---

## 十三、后续优化方向

1. **表格模板库**：预设常用表格样式
2. **数据导入**：支持 CSV/Excel 导入
3. **合并单元格**：支持跨行/跨列合并（需扩展 Markdown 或使用 HTML）
4. **公式计算**：简单的表格计算功能
5. **导出功能**：导出为 CSV/HTML
6. **历史记录**：支持撤销/重做操作

---

## 十四、参考资源

- [@dnd-kit 官方文档](https://docs.dndkit.com/)
- [markdown-table 文档](https://github.com/wooorm/markdown-table)
- [Markdown 表格语法规范](https://www.markdownguide.org/extended-syntax/#tables)
- [Radix UI Dialog 文档](https://www.radix-ui.com/primitives/docs/components/dialog)

---

**文档创建时间**: 2024-12-19  
**最后更新**: 2024-12-19  
**状态**: 📋 开发计划储备
